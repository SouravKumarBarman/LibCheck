import { View, Text } from 'react-native'

const previousSearch = ({name, authors, edition}:{name:String, authors:String[], edition: number}) => {
  return (
    <View>
      <Text>previousSearch</Text>
    </View>
  )
}

export default previousSearch